<nav class="sidebar-nav">
  <div class="navbar navbar-default" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-navbar-collapse">
      	<label style="color: #fff;">Edit other items</label>
      </button>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="navbar-collapse collapse sidebar-navbar-collapse">
      <ul class="nav navbar-nav">
		    <li class="<%= "active" if controller?("listings") && action?("basics") %>"><%= link_to "Basic info", manage_listing_basics_path(@listing) %></li>
		    <li class="<%= "active" if controller?("listings") && action?("description") %>"><%= link_to "Description", manage_listing_description_path(@listing) %></li>
		    <li class="<%= "active" if controller?("listings") && action?("address") %>"><%= link_to "Address", manage_listing_address_path(@listing) %></li>
		    <li class="<%= "active" if controller?("listings") && action?("price") %>"><%= link_to "Price", manage_listing_price_path(@listing) %></li>
		    <li class="<%= "active" if controller?("listings") && action?("photos") %>"><%= link_to "Photo", manage_listing_photos_path(@listing), data:{turbolinks: false} %></li>
		    <li class="<%= "active" if controller?("listings") && action?("calendar") %>"><%= link_to "Calendar", manage_listing_calendar_path(@listing) %></li>
		    <li class="<%= "active" if controller?("listings") && action?("bankaccount") %>"><%= link_to "Bank account", manage_listing_bankaccount_path(@listing) %></li>
		    <li class="<%= "active" if controller?("listings") && action?("publish") %>"><%= link_to "Publishment status", manage_listing_publish_path(@listing) %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>